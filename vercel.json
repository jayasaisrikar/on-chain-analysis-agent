{
  "version": 2,
  "buildCommand": "pnpm install && pnpm run build && cd frontend && pnpm install && pnpm run build",
  "installCommand": "pnpm install",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs"
}