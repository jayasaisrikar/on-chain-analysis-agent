{
  "version": 2,
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "cd .. && pnpm install --frozen-lockfile && cd frontend && pnpm install --frozen-lockfile",
        "buildCommand": "cd .. && pnpm install --frozen-lockfile && cd frontend && pnpm run build"
      }
    }
  ],
  "functions": {
    "frontend/app/api/analyze/route.ts": {
      "maxDuration": 60
    },
    "frontend/app/api/coinapi/route.ts": {
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "frontend/$1"
    }
  ]
}